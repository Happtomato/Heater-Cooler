// automatically generated by arduino-cmake
#line 1 "C:/Users/domin/CLionProjects/untitled2/untitled2.ino"
#include <LiquidCrystal.h>
#include <Arduino.h>
#include <pins_arduino.h>

#line 7 "C:/Users/domin/CLionProjects/untitled2/cmake-build-debug/untitled2_untitled2.ino.cpp"
#include "Arduino.h"

//=== START Forward: C:/Users/domin/CLionProjects/untitled2/untitled2.ino
 void setup() ;
 void setup() ;
 void loop() ;
 void loop() ;
 float roundTemp(float val, byte dec) ;
 float roundTemp(float val, byte dec) ;
 boolean checkTime();
 boolean checkTime();
 void setActors(int speedH, int speedC, int ledGruen, int ledRot,  int ledBlau);
 void setActors(int speedH, int speedC, int ledGruen, int ledRot,  int ledBlau);
//=== END Forward: C:/Users/domin/CLionProjects/untitled2/untitled2.ino
#line 3 "C:/Users/domin/CLionProjects/untitled2/untitled2.ino"


LiquidCrystal lcd(12, 11, 5, 4, 3, 2);
int sensorPin = A0;
int motorPinKalt = A5;
int motorPinHeiss = A4;

int LED_ROT = 10;
int LED_GRUEN = 9;
int LED_BLAU = 8;
int valueOld = 0;
int s = 0;
int m = 0;
int h = 0;


void setup()
{
    pinMode(LED_ROT, OUTPUT);
    pinMode(LED_GRUEN, OUTPUT);
    pinMode(LED_BLAU, OUTPUT);
    Serial.begin(9600);

    lcd.begin(16, 2);
}

void loop() {


    int sensorWert = analogRead(A0);
    float voltage = sensorWert * 5.0;
    voltage /= 1024.0;
    float temp = (voltage - 0.5) * 100 ;

    temp = roundTemp(temp, 0);

    lcd.setCursor(0, 1);
    lcd.print("T:");

    lcd.setCursor(3, 1);
    lcd.print(temp);

    valueOld = temp;

    boolean run = checkTime;


    while(!run){

        //turn on led for respective case
        if(temp < 24 && temp > 22){
            setActors(0,0,1,0,0);

        }

        else if(temp > 24){
            setActors(0,1000,0,0,1);
        }

        else if(temp < 22){
            setActors(1000,0,0,1,0);
        }
    }
    delay (1000);
    s++;

    if(s == 60){
        lcd.clear();
        m++;
        s = 0;
    }
    if(m == 60){
        lcd.clear();
        h++;
        m = 0;
    }
    if(h == 24){
        lcd.clear();
        h = 0;
    }

    lcd.setCursor(0,0);


    lcd.print("Zeit: " );
    if( h >= 1){
        lcd.print(h);
    }
    else if(h == 0){
        lcd.print("00");
    }
    lcd.print(":");
    if(m >= 1){
        lcd.print(m);
    }
    else if(m == 0){
        lcd.print("00");
    }
    lcd.print(":");
    if(s >= 1){
        lcd.print(s);
    }
    else if(h == 0){
        lcd.print("00");
    }

    /*
    else{
      int geschwindigkeitH = 0;
      int geschwindigkeitK = 0;
        digitalWrite(LED_GRUEN,LOW);
        digitalWrite(LED_ROT, LOW);
        digitalWrite(LED_BLAU, LOW);
      analogWrite(motorPinHeiss, geschwindigkeitH);
      analogWrite(motorPinKalt, geschwindigkeitK);
      Serial.println("GeschwindigkeitH :");
          Serial.print(geschwindigkeitH);
      Serial.println("GeschwindigkeitK :");
          Serial.print(geschwindigkeitK);
      delay(1000);
    }
    */

}

float oldTemp;
int oldX;
float roundTemp(float val, byte dec) {

    if(val != oldTemp){

        float x = val * pow(10, dec);
        float y = round(x);
        float z = x - y;
        if ((int) z == 5) {
            y++;
        } else {
        }

        oldTemp = val;


        x = y / pow(10, dec);

        oldX = x;

        return x;
    }
    return oldX;
}


boolean checkTime(){
    if(h > 8 && m == 0 || h < 17 && m == 59){
        return true;
        Serial.print("true");
    }
    return false;
    Serial.print("false");
}

void setActors(int speedH, int speedC, int ledGruen, int ledRot,  int ledBlau){
    int geschwindigkeitH = speedH;
    int geschwindigkeitK = speedC;
    analogWrite(motorPinHeiss, geschwindigkeitH);
    analogWrite(motorPinKalt, geschwindigkeitK);
    digitalWrite(LED_GRUEN,ledGruen);
    digitalWrite(LED_ROT, ledRot);
    digitalWrite(LED_BLAU, ledBlau);

}